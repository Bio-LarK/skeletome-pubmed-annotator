'use strict';

/**
 * @ngdoc function
 * @name skeletomePubmedAnnotatorApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the skeletomePubmedAnnotatorApp
 */
angular.module('skeletomePubmedAnnotatorApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
