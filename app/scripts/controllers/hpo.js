'use strict';

/**
 * @ngdoc function
 * @name skeletomePubmedAnnotatorApp.controller:HpoCtrl
 * @description
 * # HpoCtrl
 * Controller of the skeletomePubmedAnnotatorApp
 */
angular.module('skeletomePubmedAnnotatorApp')
  .controller('HpoCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
